int arr[]={10,20,30,40,50};
      int left=0;
      int rigth=arr.length-1;
      while(left<rigth){
        int temp=arr[left];
        arr[left]=arr[rigth];
        arr[rigth]=temp;
        left++;
        rigth--;
      }
      for(int i=0;i<arr.length;i++){
        System.out.println(arr[i]);
      }
    }
